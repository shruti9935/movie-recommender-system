# Read by st.secrets, both locally and on Streamlit Community Cloud.
#
# This key is committed deliberately so the deployed app needs no configuration.
# It is therefore public: treat it as disposable. To swap it, change the line
# below and redeploy, or override it in Streamlit under Settings > Secrets
# (the dashboard value wins over this file).
TMDB_API_KEY = "f85dd0c4db54d280e041f62e63df9401"
