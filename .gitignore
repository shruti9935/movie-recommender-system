# local env file — not used for deployment, keep it out of the repo
.env
.env.*
!.env.example

# NOTE: .streamlit/secrets.toml IS committed on purpose. The TMDB key is public
# and disposable, so the deployed app works with no dashboard configuration.

# python
__pycache__/
*.py[cod]
.venv/
venv/

# IDE
.idea/
.vscode/

# datasets — ~45 MB, needed only to rebuild the model (see README)
tmdb_5000_movies.csv
tmdb_5000_credits.csv
